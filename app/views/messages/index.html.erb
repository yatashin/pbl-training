<!-- app/views/messages/index.html.erb -->
<h1>匿名掲示板</h1>

<%= link_to "新規投稿", new_message_path %>

<ul>
  <% @messages.each do |message| %>
    <li>
      <strong>投稿日:</strong> <%= message.created_at.strftime("%Y-%m-%d %H:%M") %><br>
      <%= message.content %>
    </li>
  <% end %>
</ul>
